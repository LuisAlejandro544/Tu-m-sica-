[package]
name = "spotlocal_rust_parser"
version = "0.1.0"
edition = "2021"
authors = ["SpotLocal Developer"]
description = "Rust ultra-fast memory-safe audio metadata parser (ID3, FLAC, OGG, WAV) for Android"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
jni = "0.21"
lofty = "0.19"
